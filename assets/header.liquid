<div class="sn-header">
  <div class="content">
    <div class="side">
      <img class="mobile" id="hamburger-icon"src="{{ 'Hamburger.png' | asset_url }}" height="auto" width="auto"/>
    </div>
    <div class="main">
      <a href="{{ routes.root_url }}">
        <img id="logo" src="{{ 'SRN-BLACK-1.png' | asset_url }}" height="auto" width="auto"/>
      </a>
      <div class="menu">
        <div><a href="{{ collections['web-shop'].url }}">THE SHOP</a></div>
        <div><a href="/pages/nursery">THE NURSERY</a></div>
        <div><a href="/pages/garden-design">GARDEN DESIGN</a></div>
      </div>
    </div>
    <div class="side">
      <img id="sort-icon" src="{{ 'WormFilterIcon.png' | asset_url }}" height="auto" width="auto"/>
    </div>
  </div>
  <p class="test-bold">This is a test of Bold style</p>
<p class="test-italic">This is a test of Italic style</p>
<p class="test-condensed">This is a test of Condensed style</p>
<p class="test-extended">This is a test of Extended style</p>
<p class="test-medium-extended">This is a test of Medium Extended style</p>
</div>
<style>
  .test-bold {
  font-family: var(--font-family-secondary);
  font-weight: 700;
  font-style: normal;
}

.test-italic {
  font-family: var(--font-family-secondary);
  font-weight: 400;
  font-style: italic;
}

.test-condensed {
  font-family: var(--font-family-secondary);
  font-weight: 400;
  font-style: normal;
  font-stretch: condensed;
}

.test-extended {
  font-family: var(--font-family-secondary);
  font-weight: 400;
  font-style: normal;
  font-stretch: expanded;
}

.test-medium-extended {
  font-family: var(--font-family-secondary);
  font-weight: 500;
  font-style: normal;
  font-stretch: expanded;
}
  a {
    text-decoration: none;
    color: #3B5669;
  }
  a:visited {
    color: #3B5669;
  }
  #logo {
    height: 180px;
    margin: 25px 0px;
  }
  #filter {
    margin-top: 10px;
  }
  #menu-icon {
    margin-top: 10px;
  }
  .content {
    max-width: 1200px;
    width: 100%;
    min-width: 768px;
    margin-left: auto;
    margin-right: auto;
    display: flex;
  }
  .side {
    min-width: 20%;
    max-width: 275px;
    min-height: 1px;
    display: flex;
    flex-direction: column-reverse;
    align-items: center;
  }
  .main {
    display: flex;
    flex-direction: column;
    align-items: center;
    flex: 1;
  }
  .sn-header {
    width: 100vw;
  }
  .menu {
    font-family: var(--font-family-secondary);
    font-size: 14px;
    max-width: 768px;
    width: 80%;
    height: 50px;
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-start;
  }

  .side:first-child {
    display: flex !important;
  }

  .mobile {
    display: none;
  }

  #hamburger-icon {
    height: 20px
  }

  #sort-icon {
    height: 60px
  }

  @media (max-width: 768px) {
    #logo {
      height: 90px;
      margin: 25px 0px;
    }
    
    #hamburger-icon {
      height: 20px
    }
    
    #sort-icon {
      height: 45px
    }

    .content {
      min-width: auto;
      padding: 0 20px;
      align-items: center;
    }

    .side {
      min-width: auto;
      max-width: none;
      width: 60px;
    }
    
    .menu {
      display: none;
    }

    .mobile {
      display: block;
    }
  }
</style>