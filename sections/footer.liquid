<div id="footer" class="{% if page.handle == 'nursery' %}nursery-footer{% elsif page.handle == 'garden-design' %}garden-design-footer{% endif %}">
  <div id="footer-content">
    <div class="footer-side">
      <div class="footer-menu">
        <a href="{{ collections['web-shop'].url }}">THE SHOP</a>
        <a href="/pages/nursery">THE NURSERY</a>
        <a href="/pages/garden-design">GARDEN DESIGN</a>
        <a href="/pages/nursery#contact">CONTACT US</a>
        <a href="https://www.instagram.com/springroadnursery/">INSTAGRAM</a>
      </div>
    </div>
    <div id="inchworm-logo">
      {% if page.handle == 'nursery' %}
        <img src="{{ 'inchworm-black.png' | asset_url }}" height="150px" width="auto"/>
      {% elsif page.handle == 'garden-design' %}
        <img src="{{ 'inchworm-light-green.png' | asset_url }}" height="150px" width="auto"/>
      {% else %}
        <img src="{{ 'inchworm.png' | asset_url }}" height="150px" width="auto"/>
      {% endif %}
    </div>
    <div class="footer-side">
      <div class="side-title wavy-underline">
        Keep In touch
      </div>
      <div class="side-text">
        From rare plants to handcrafted goods,
        we cultivate the strange and the beautiful
        on our mountainside in Vermont.  
        Be the first to hear about 
        what's unfurling in the garden.
      </div>
      <div id="footer-email-form">
        {% form 'customer', class: 'newsletter-form' %}
        {%- if form.posted_successfully? -%}
          <h3
            class="newsletter-form__message newsletter-form__message--success"
            id="Newsletter-success--{{ section.id }}"
            autofocus
          >
            {{- 'newsletter.success' | t }}
          </h3>
        {%  else %}
          <input type="hidden" name="contact[tags]" value="newsletter">
          <div class="newsletter-form__field-wrapper">
            <div class="field">
              <input
                id="NewsletterForm--{{ section.id }}"
                type="email"
                name="contact[email]"
                class="field__input"
                value="{{ form.email }}"
                aria-required="true"
                autocorrect="off"
                autocapitalize="off"
                autocomplete="email"
                {% if form.errors %}
                  autofocus
                  aria-invalid="true"
                  aria-describedby="Newsletter-error--{{ section.id }}"
                {% elsif form.posted_successfully? %}
                  aria-describedby="Newsletter-success--{{ section.id }}"
                {% endif %}
                placeholder="Your Email Here"
                required
              >
              <label class="newsletter-label field__label" for="NewsletterForm--{{ section.id }}">
                Your Email Address Here
              </label>
              <button
                type="submit"
                class="newsletter-form__button field__button"
                name="commit"
                id="Subscribe"
                aria-label="{{ 'newsletter.button_label' | t }}"
              >
                <span class="svg-wrapper">
                  {{- 'icon-arrow.svg' | inline_asset_content -}}
                </span>
              </button>
            </div>
            {%- if form.errors -%}
              <small class="newsletter-form__message form__message" id="Newsletter-error--{{ section.id }}">
                <span class="svg-wrapper">
                  {{- 'icon-error.svg' | inline_asset_content -}}
                </span>
                {{- form.errors.translated_fields.email | capitalize }}
                {{ form.errors.messages.email -}}
              </small>
            {%- endif -%}
          </div>
        {%- endif -%}
      {% endform %}
      </div>
    </div>
    <div id="footer-email-form-mobile">
        {% form 'customer', class: 'newsletter-form' %}
        {%- if form.posted_successfully? -%}
          <h3
            class="newsletter-form__message newsletter-form__message--success"
            id="Newsletter-success--{{ section.id }}"
            autofocus
          >
            {{- 'newsletter.success' | t }}
          </h3>
        {%  else %}
          <input type="hidden" name="contact[tags]" value="newsletter">
          <div class="newsletter-form__field-wrapper">
            <div class="field">
              <input
                id="NewsletterForm--{{ section.id }}"
                type="email"
                name="contact[email]"
                class="field__input"
                value="{{ form.email }}"
                aria-required="true"
                autocorrect="off"
                autocapitalize="off"
                autocomplete="email"
                {% if form.errors %}
                  autofocus
                  aria-invalid="true"
                  aria-describedby="Newsletter-error--{{ section.id }}"
                {% elsif form.posted_successfully? %}
                  aria-describedby="Newsletter-success--{{ section.id }}"
                {% endif %}
                placeholder="Your Email Here"
                required
              >
              <label class="newsletter-label field__label" for="NewsletterForm--{{ section.id }}">
                Your Email Here
              </label>
              <button
                type="submit"
                class="newsletter-form__button field__button"
                name="commit"
                id="Subscribe"
                aria-label="{{ 'newsletter.button_label' | t }}"
              >
                <span class="svg-wrapper">
                  {{- 'icon-arrow.svg' | inline_asset_content -}}
                </span>
              </button>
            </div>
            {%- if form.errors -%}
              <small class="newsletter-form__message form__message" id="Newsletter-error--{{ section.id }}">
                <span class="svg-wrapper">
                  {{- 'icon-error.svg' | inline_asset_content -}}
                </span>
                {{- form.errors.translated_fields.email | capitalize }}
                {{ form.errors.messages.email -}}
              </small>
            {%- endif -%}
          </div>
        {%- endif -%}
      {% endform %}
      </div>
  </div>
  <div id="footer-details">
    <span>Spring Road Nursery </span>      
    <span>78 Spring Rd </span>      
    <span>Tunbridge, VT 05077  </span>      
    <span>(802) 276-6481</span>
  </div>
</div>
<style>
    #footer {
      margin: 50px 0;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      color: var(--font-color-dark);
      font-family: var(--font-family-secondary);
    }
    #footer a, #footer a:visited {
      color: var(--font-color-dark);
    }
    .nursery-footer .menu a,
    .nursery-footer .menu a:visited{
      color: var(--font-color-dark-secondary) !important;
    }
    .garden-design-footer .menu a,
    .garden-design-footer .menu a:visited{
    color: var(--font-color-light-quaternary) !important;
  }
    h3 {
      text-align: center;
    }
    #footer-content {
      min-width: 700px;
      max-width: 1100px;
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .footer-side {
      width: 33%;
      text-align: center;
    }

    .side-title {
      font-weight: 500;
      font-size: 24px;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-decoration-thickness: 1px; /* Set the thickness of the underline */
      padding: 0 50px;
    }

    .side-text {
      padding: 0 50px;
      margin-top: 10px;
      margin-bottom: 20px;
      font-size: 15px;
      line-height: 1;
    }
    #footer .newsletter-label {
      font-size: 14px;
      font-stretch: expanded;
      text-transform: uppercase;
      font-stretch: expanded;
    }
    .footer-menu {
      font-size: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      letter-spacing: 2px;
      gap: 5px;
      text-transform: uppercase;
      font-stretch: expanded;
    }
    #footer-details {
      margin-top: 65px;
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      gap: 25px;
      text-transform: uppercase;
      font-size: 12px;
      font-stretch: expanded;
    }

    #footer-email-form {
      margin-top: 40px;
    }

    #footer-email-form-mobile {
      display: none;
    }

    .nursery-footer,
    .nursery-footer a,
    .nursery-footer a:visited,
    .nursery-footer span,
    .nursery-footer .side-title,
    .nursery-footer .side-text,
    .nursery-footer .field__label,
    .nursery-footer .field__input {
      color: var(--font-color-dark-secondary) !important;
    }

    .garden-design-footer,
    .garden-design-footer a,
    .garden-design-footer a:visited,
    .garden-design-footer span,
    .garden-design-footer .side-title,
    .garden-design-footer .side-text,
    .garden-design-footer .field__label,
    .garden-design-footer .field__input {
      color: var(--font-color-light-quaternary) !important;
    }

    @media(max-width: 990px) {
      #inchworm-logo {
        display: none;
      }
      .footer-side {
        width: 50%;
        padding: 0 30px;
      }
    }

@media(max-width: 768px) {
  #footer-content {
    min-width: 0;
    width: 100%;
    flex-direction: column;
    align-items: center; /* Keep items centered */
  }

  .footer-side {
    width: 100%; /* Change from 50% to 100% */
    max-width: 425px; /* Match the form max-width for consistency */
    padding: 0 20px; /* Add some breathing room */
    margin-bottom: 30px; /* Add spacing between sections */
  }

  .footer-menu {
    width: 100%; /* Let it use full available width */
    font-size: 12px;
    padding: 0;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 10px 15px; /* Add horizontal gap for better spacing */
    justify-content: center;
  }

  .side-title, .side-text {
    width: 100%; /* Use full width instead of fixed 320px */
    padding: 0 20px; /* Consistent padding */
  }

  #footer-email-form {
    display: none;
  }

#footer-email-form-mobile {
  display: block;
  width: 100%;
  max-width: none;
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
#footer-email-form-mobile .field {
  width: 100%;
  max-width: none;
  padding: 0;
  margin: 0;
}

#footer-email-form-mobile .field__input {
  width: 100%;
}
  #footer-details {
    display: flex; 
    flex-wrap: wrap;
    align-items: center;
    justify-content: center; /* Center the items */
    font-size: 10px;
    gap: 5px;
  }

  #footer-details span:first-child,
  #footer-details span:last-child {
    text-align: center;
    flex-basis: 100%; /* Force these to take full width, creating their own row */
  }


#footer-email-form-mobile .field__input {
  background-color: var(--font-color-dark); /* Use the text color as background */
  color: var(--font-color-dark); /* Use the background color as text */
}

#footer-email-form-mobile .field__label {
  color: var(--color-background); /* Make label match the input text color */
}

#footer-email-form-mobile .field__input::placeholder {
  opacity: 0.7; /* Optional: make placeholder slightly transparent */
}
.nursery-footer #footer-email-form-mobile .field__input {
  background-color: var(--font-color-dark-secondary);
}

.nursery-footer #footer-email-form-mobile .field__input {
  background-color: var(--font-color-dark-qu);
}

.garden-design-footer #footer-email-form-mobile .field__input {
  background-color: var(--font-color-light-quaternary);
  }

  #footer-email-form-mobile .svg-wrapper,
#footer-email-form-mobile .svg-wrapper svg {
  color: var(--color-background-light);
  fill: var(--color-background-light);
}
.nursery-footer #footer-email-form-mobile .svg-wrapper,
.nursery-footer #footer-email-form-mobile .svg-wrapper svg {
  color: var(--color-background-light);
  fill: var(--color-background-light);
}

.garden-design-footer #footer-email-form-mobile .svg-wrapper,
.garden-design-footer #footer-email-form-mobile .svg-wrapper svg {
  color: var(--color-background-light);
  fill: var(--color-background-light);
}
}

</style>